{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run (Linux)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/invaders_linux",
            "args": [],
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
        }
    ]
}